# Examples

Run them from the workspace root:

```sh
cargo run -p excelx-examples --bin manual_read_write
cargo run -p excelx-examples --bin derive_read_write
cargo run -p excelx-examples --bin multi_sheet
```

Each example writes an `.xlsx` file to `target/examples/`.
