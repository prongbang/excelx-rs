//! Placeholder crate for the Phase 3 `#[derive(ExcelRow)]` implementation.
//!
//! The core `excelx` crate intentionally ships first with a stable manual trait
//! API before proc-macro ergonomics are added.
